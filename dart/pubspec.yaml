name: apiverve_initialismgenerator
description: Initialism Generator creates initialisms from phrases by extracting first letters of significant words, with options for filtering and formatting.
version: 1.1.14
homepage: https://apiverve.com/marketplace/initialismgenerator?utm_source=dart&utm_medium=homepage
repository: https://github.com/apiverve/initialismgenerator-api/tree/main/dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  http_parser: ^4.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
